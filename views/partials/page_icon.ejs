<div style="display:flex; align-items:center; gap:12px; margin-bottom:10px;">
  <img src="/assets/icons/icon-192.png" alt="<%= appName %>" style="width:44px; height:44px; border-radius:14px; border:1px solid rgba(255,255,255,.12);" />
  <div>
    <div style="font-weight:900; letter-spacing:.2px;"><%= heading %></div>
    <% if (subheading) { %>
      <div class="small"><%= subheading %></div>
    <% } %>
  </div>
</div>
